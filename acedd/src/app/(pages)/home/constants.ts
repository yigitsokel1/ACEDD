/**
 * Home Page Constants
 * 
 * Sprint 11: Content data moved to settings (content.home.*)
 * 
 * This file now only exports types for component props.
 * All content data is managed via Admin UI and stored in settings.
 */

// Re-export types for component props (if needed)
export type { StatCard, MissionCard, ActivityCard, TrustIndicator, HeroSection } from './types';
