# Database connection
# MariaDB connection string format: mysql://user:password@host:port/database
DATABASE_URL="mysql://user:password@host:port/db_name"

# Shadow database (opsiyonel - sadece Plesk'te CREATE DATABASE yetkisi yoksa gerekli)
# Prisma otomatik shadow DB oluşturmaya çalışır, yetki yoksa manuel oluşturup buraya ekleyin
# Plesk → Databases → "+ Add Database" → `acedd_shadow` oluşturun
# SHADOW_DATABASE_URL="mysql://user:password@host:port/acedd_shadow"

# Note: Şu an bu DB geliştirme ve ilk yayın için kullanılıyor.
# İlk prod release'ten sonra bu DB prod sayılacak ve ayrı dev DB açılacak.

# Admin Session Security (Sprint 6: HMAC-SHA256)
# Generate with: npm run generate-session-secret
# Use different secrets for development and production
SESSION_SECRET="change-me-in-production-generate-with-npm-run-generate-session-secret"

# Base URL for internal API calls (Sprint 9: Dashboard)
# Development: http://localhost:3000
# Production: https://yourdomain.com (MUTLAKA doldur!)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
